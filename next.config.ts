// next.config.ts

const nextConfig = {
  // Ваши другие настройки...

  // Добавьте эту строку:
  experimental: {
    // Можете удалить 'turbo' отсюда, так как оно уже не нужно в Next 16
  },
};

export default nextConfig;
